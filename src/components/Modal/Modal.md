
modal:
```js

<Toggle>
{({ on, toggle }) => (
  <div>
    <Button onClick={toggle}>Modal</Button>
    <Modal on={on} toggle={toggle}>
      <h1>Still what's up this is scott</h1>
    </Modal>
  </div>
)}
</Toggle>
```
